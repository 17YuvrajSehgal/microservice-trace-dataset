# TODO — 31 Jul 2026

Driving from `dataset-collection-gaps.md`. Focus this session: **start the dataset-side
changes (wave-2 collection) and test them.**

## Dataset — wave-2 collection changes (MSR-critical)

### Implement (this session)
- [x] **Memory-augmented kernel profile** — `KERNEL_MEM=1` knob in `collect_trace.sh`.
      **VM-validated:** captures `writeback_*` (216K+ events under pressure) + enables
      `mm_vmscan_*` (reclaim, fires under sustained pressure). Note this kernel has NO
      `kmem_*` tracepoints; the real memory signal is `mm_vmscan_*` + `writeback_*`.
      *(unblocks F3/F8 kernel evidence + the memory-leak/gc agentic skills)*
- [x] **Host disk / memory / network stressor recipes** — `faults/host_disk.sh`,
      `faults/host_mem.sh`, `faults/host_net.sh`. **VM-validated:** all three inject→cleanup
      cleanly, emit valid ground truth; host_net applies+removes netem/tbf on the bridge.
      *(RQ1's core "confusable resource faults" set)*
- [x] **Full Prometheus export** — `download_metrics_full.sh` ALREADY EXISTS (Jul 25, sweeps
      the whole `__name__` space, gzipped per-metric). Gap is only *wiring it into the run
      driver* + confirming it ran in v1 (else re-export while Prom retention holds).
- [x] **Per-container netem (F12)** — `faults/svc_net.sh` via `nsenter` into the target's
      netns (TARGET_SVC=carts). Syntax OK.
- [x] **Renamed** host_disk/mem/net → `anomaly_disk/mem/net` to match the EXISTING
      `verification_targets.json` entries + the `anomaly_cpu` sibling.
- [x] **verification targets** — `anomaly_disk/mem/net` already existed; added `svc_net_carts`,
      relaxed `anomaly_mem` threshold to a safe ~78% aggressive. JSON valid.
- [x] **`collect_wave2.sh`** — one-command orchestrator (wraps `run_scenario.sh`, sets
      `KERNEL_MEM`/`TARGET_SVC` per row). Dry-run = 15-run core plan (host disk/net + mem-aug
      anomaly_mem + svc_mem_cap + svc_net); `subtle` group adds RQ5 runs.
      **Note:** `run_scenario.sh` ALREADY calls `download_metrics_full.sh` → wave-2 (and v1)
      get full metrics automatically. **Wave-2 is now one command:** `./collect_wave2.sh`.

### Test
- [x] `bash -n` on all new/changed scripts — clean.
- [x] Ground-truth JSON contract for all recipes — valid, window opens+closes.
- [x] VM smoke test — mem profile captures `writeback_*`; recipes run+clean; no CRLF.
- [x] **Full wave-2 validation run (REPEATS=1, stack up)** — pipeline PERFECT: audit
      six-OK (trace/logs/load/metrics/kernel/clocks), clock drift 0.003 ms, 428 full-metric
      series, 4.5M kernel events; KERNEL_MEM passthrough works.
      **Verdicts after calibration:** anomaly_disk ✓ · anomaly_net ✓ · svc_mem_cap ✓ ·
      svc_net ✓ (carts p95 0.04→9.9s) · anomaly_mem (see below).

### Calibration findings from the run (fixes applied + committed)
- **anomaly_disk**: io_time fired 16× (0.04→0.66) but under the 0.8 gate → relaxed to 0.5 +
  added O_DIRECT to the recipe. Now **confirmed**.
- **anomaly_net**: `tc netem` on the *bridge device* does NOT impair L2-forwarded
  container↔container traffic (catalogue p95 stayed flat). Rewrote to per-container-netns
  netem across the stack (svc_net proved per-netns works) + changed the target from
  throughput → catalogue p95. Now **confirmed**.
- **svc_net**: confirmed (carts p95 → 9.9s).
- **anomaly_mem — ✅ CONFIRMED (bigheap).** `host_mem_available_drop` 0.837 → **0.147**
  (pass). During injection MemAvail collapsed 33 G→~4.8 G (~12%) with swap climbing to
  5–7 G — the cgroup cap cycles (fill→reclaim→refill), driving active reclaim.
  **How we got here:** `--vm` capped at ~7–8 GB (churns, never holds) even after 16 GB swap
  + absolute bytes + `--vm-hang`; MemAvail only reached 0.63 — but the **F3 kernel signature
  already fired there** (`mm_vmscan_lru_isolate` 104K etc.). Switched to **`stress-ng
  --bigheap N` in a `--memory`-capped container**: cap = target host RAM → fills to the cap
  (drops MemAvail) and hitting the cap forces reclaim + swap; bounded so it can't OOM the
  stack. Confirmed on the VM. Swap persisted in `/etc/fstab`.
- **→ 5/5 wave-2 faults now confirmed: anomaly_disk · anomaly_net · anomaly_mem ·
  svc_mem_cap · svc_net.**

### RQ4 overhead
- [x] **`collect_overhead.sh`** — fair rotated wrapper (baseline/lttng_only/lmat_async),
      auto-detects LMAT model, falls back to baseline+lttng_only. Dry-run OK. (No LMAT model
      on the VM → runs the kernel-overhead measurement.)

### Decisions pending (Naser) — from `dataset-collection-gaps.md`
- [ ] Memory tracepoints: small augmented subset vs. none.
- [ ] Full-Prometheus scope: whole label space vs. expanded curated set.
- [ ] Wave-2 size: full ~30 runs vs. lean must-only (~20).

### Then (Phase 3)
- [ ] L1 kernel-KPI deriver (Parquet); L2 = productionize `wait_attribution.py`; L3 NL digest.
- [ ] Loader SDK (`stratatrace`).
- [ ] Coverage matrix; confirm Tier-2 (user/payment) trace status.

## Agentic — parallel
- [ ] Validate `applog_source.py` bt2 wrapper on VM (`babeltrace-spike/run_spike.sh`).
- [ ] Adaptive-tracing skill (feedback loop).
- [ ] Wire 5th skill (cpu-saturation) → 5/5.
- [ ] Stress-test to find the gap (compound faults) → paper contribution.

## Housekeeping
- [ ] VM is TERMINATED — start only for testing, stop after (cost).
