{
  "id": "db-latency-dependency-wait",
  "version": 2,
  "title": "Callers converge on one datastore that answers slowly while sitting idle",
  "problem": {
    "summary": "Several services slow down at once and their slow call paths all end at the same downstream datastore. The datastore is not busy: its CPU, disk and memory are quiet, and it emits no request traces of its own, so ordinary telemetry points at its callers instead of at it. The callers are victims; the datastore is spending its time blocked on something outside itself.",
    "symptoms": [
      "caller latency inflates sharply, often by orders of magnitude",
      "the slow call edges all converge on one shared downstream component",
      "that component is resource-quiet: CPU flat or falling, no disk or memory pressure",
      "requests still succeed, so error rates barely move"
    ],
    "discriminators": [
      {
        "signal": "direction of the slow call edges",
        "this_problem": "the suspect has slow INCOMING edges while its own outgoing edges are normal, or it emits no spans at all",
        "not_this_problem": "its outgoing edges are also slow, meaning the real cause is further downstream",
        "confused_with": [],
        "evidence": "Topology convergence is computed per run by scripts/edge_convergence.py; see results/."
      },
      {
        "signal": "how the off-CPU time splits (family_seconds), not the coarse wait shares",
        "this_problem": "idle_epoll is the largest single share, averaging 42.9% for this family",
        "not_this_problem": "blocked_futex dominates and idle_epoll is small, e.g. 3.9% for a service pinned by its own CPU limit",
        "confused_with": [
          "service-cpu-throttle",
          "cpu-contention-co-tenant"
        ],
        "evidence": "MEASURED across 14 runs of this family, 93 runs total. CAUTION: stdev is +-30.8 points, i.e. larger than the 18.5-point gap to the next family, so this separates on the mean but is NOT reliable per-incident. See evidence/wait_signature_all_families.json"
      }
    ],
    "unverified_do_not_claim": [
      {
        "claim": "the converged-on component shows dominant off-CPU external I/O wait, and that is what identifies it",
        "status": "TRUE BUT NOT DISCRIMINATIVE",
        "measurement": "off_cpu_io_wait is 98-99% for EVERY fault family measured (slow_db 99.4, dependency_outage 98.2, svc_cpu_cap 98.0). The coarse buckets are dominated by idle waiting, so they cannot separate anything. See evidence/wait_signature_all_families.json",
        "consequence": "replaced by the family_seconds split, which is where any real signal lives.",
        "agent_note": "A dominant off-CPU external-I/O share on the suspect. Measured at 98-99% for EVERY fault type we have labelled, because the coarse buckets are dominated by ordinary idle waiting. It is true here and equally true everywhere, so it identifies nothing."
      }
    ]
  },
  "reproduction": {
    "system": "Sock Shop or Train Ticket on Ubuntu 24.04, LTTng 2.15, Docker 27+, single GCP n2 VM. Toxiproxy sits permanently in the datastore path so latency can be toggled without restarting anything.",
    "recipe": "microservice-lttng-data-collection-scripts/faults/slow_db.sh",
    "invoke": "./slow_db.sh inject aggressive",
    "teardown": "./slow_db.sh cleanup",
    "labelled_examples": [
      "sockshop/slow_db/slow_db_aggressive_steady_r1",
      "sockshop/slow_db/slow_db_subtle_steady_r1",
      "trainticket/slow_db/tt_slow_db_aggressive_steady_r1"
    ],
    "harness_only": [
      "labelled_examples"
    ]
  },
  "collection_order": {
    "window": {
      "baseline_s": 60,
      "incident_s": 120,
      "recovery_s": 60
    },
    "kernel_events": [
      "sched_switch",
      "sched_waking",
      "syscall_entry_recvfrom",
      "syscall_exit_recvfrom",
      "syscall_entry_epoll_wait",
      "syscall_exit_epoll_wait",
      "syscall_entry_futex",
      "syscall_exit_futex"
    ],
    "metrics": [
      "container_cpu_usage_seconds_total",
      "container_fs_reads_bytes_total",
      "container_fs_writes_bytes_total",
      "container_network_receive_bytes_total"
    ],
    "logs": [
      "error-rate change per container, to confirm calls succeed rather than fail"
    ],
    "traces": [
      "caller to callee edge latency, baseline versus incident, so convergence can be seen"
    ],
    "why_these": "Unlike the CPU-contention blueprint, this one DOES need syscall entry and exit events: the verdict depends on which syscall the thread was blocked in when it left the CPU. sched_switch alone tells you it stopped running; the open syscall tells you it stopped to wait on the network or a socket rather than on disk or a lock. That distinction is the whole diagnosis, so the cheaper scheduler-only order is not sufficient here."
  },
  "processing": [
    {
      "step": "Stage the kernel trace so babeltrace2 can read it, because channels are stored gzipped",
      "run": "cp -r <run_dir>/kernel/kernel <tmp>/ctf && gunzip -f <tmp>/ctf/*.gz",
      "produces": "<tmp>/ctf",
      "expect": "a CTF directory containing a metadata file and channel0_* streams"
    },
    {
      "step": "Find which downstream component the slow call edges converge on",
      "run": "python3 blueprints/lib/edge_convergence.py --run <run_dir> --out <out>/convergence.json",
      "reads": [
        "<run_dir>/otlp"
      ],
      "produces": "<out>/convergence.json",
      "expect": "one component with slow incoming edges and no slow outgoing edges; if several, take the deepest"
    },
    {
      "step": "Decode scheduler and syscall events for the suspect inside the incident window",
      "run": "TZ=UTC babeltrace2 <tmp>/ctf --begin <incident_start_utc> --end <incident_end_utc> | grep -E \"sched_switch|sched_waking|syscall_entry_|syscall_exit_\" > <tmp>/waits.txt",
      "reads": [
        "<tmp>/ctf"
      ],
      "produces": "<tmp>/waits.txt",
      "expect": "non-empty output; zero lines means the window is misaligned, because the trace is stamped in the host timezone while ground truth is UTC, so TZ=UTC is required"
    },
    {
      "step": "Attribute the suspect wall time across on-CPU, runnable-wait, disk-wait and external I/O wait",
      "run": "python3 stratatrace/derive_kernel_l2.py <run_dir> --services <suspect> --out <out>/wait.jsonl",
      "reads": [
        "<run_dir>/kernel",
        "<run_dir>/meta"
      ],
      "produces": "<out>/wait.jsonl",
      "expect": "off_cpu_io_wait dominant, on_cpu near zero, and a verdict_hint of external_io_or_dependency_wait"
    },
    {
      "step": "Confirm the suspect is unsaturated, then emit the verdict and its artifacts",
      "run": "python3 blueprints/lib/dependency_verdict.py --run <run_dir> --convergence <out>/convergence.json --wait <out>/wait.jsonl --out <out>/verdict.json --chart <out>/wait_shares.svg --text <out>/explanation.txt",
      "reads": [
        "<out>/convergence.json",
        "<out>/wait.jsonl"
      ],
      "produces": "<out>/verdict.json, <out>/wait_shares.svg, <out>/explanation.txt",
      "expect": "the suspect shows external wait without CPU, disk or memory saturation"
    }
  ],
  "outputs": [
    {
      "kind": "json",
      "path": "<out>/verdict.json",
      "contains": "the converged-on component, its wait decomposition, the slowdown factor on its incoming edges, and the resource signals that rule out saturation"
    },
    {
      "kind": "xy_chart",
      "path": "<out>/wait_shares.svg",
      "contains": "the four wait shares for the suspect over time, with the incident window shaded"
    },
    {
      "kind": "text",
      "path": "<out>/explanation.txt",
      "contains": "which component was waiting, on what, and why its callers are victims rather than causes"
    }
  ],
  "decision": {
    "verdict_when": [
      "slow call edges converge on one component that has no slow outgoing edges",
      "that component shows dominant off-CPU external I/O wait with a near-zero on-CPU share",
      "it is resource-quiet: no CPU, disk or memory saturation",
      "traffic continues to succeed, so error rates barely move"
    ],
    "rule_out": [
      {
        "instead": "a frozen dependency",
        "when": "calls fail or hang to timeout and the component serves little successful traffic"
      },
      {
        "instead": "host disk saturation",
        "when": "block latency is up host-wide and other disk users degrade too"
      },
      {
        "instead": "CPU starvation",
        "when": "the suspect shows runnable_wait rather than external I/O wait"
      },
      {
        "instead": "network degradation",
        "when": "every hop slows by a similar factor, not just the datastore path"
      }
    ],
    "root_cause_is": "the converged-on datastore component itself, never its callers, which are victims",
    "fault_type": "slow_db"
  },
  "evidence_from_literature": [
    {
      "claim": "Blocking-syscall attribution distinguishes a component that is slow from a component that is waiting, which utilisation metrics cannot do",
      "source": "off-CPU analysis practice (Gregg)",
      "validated_on": "our labelled slow-datastore runs; on Sock Shop this verdict survives even with traces removed entirely"
    }
  ],
  "related_blueprints": [
    "cpu-contention-co-tenant"
  ],
  "mutually_exclusive_with": [
    "cpu-contention-co-tenant"
  ],
  "provenance": {
    "authored_by": "human",
    "verified_by": "PENDING - Naser to sign off",
    "authored_utc": "2026-08-27",
    "notes": "v2: the original 'off_cpu_io_wait dominates' discriminator was measured to be true of every family, so it identified nothing. Replaced with the finer family_seconds split, and the remaining separation is reported with its variance because the spread is wider than the gap."
  }
}