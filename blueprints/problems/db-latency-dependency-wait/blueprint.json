{
  "id": "db-latency-dependency-wait",
  "version": 5,
  "title": "A datastore blocks in its socket while idle; its callers converge on it",
  "problem": {
    "summary": "Several services slow down and their slow call paths all end at the same downstream component. That component is not busy and is not short of CPU - it is blocked inside a single socket-waiting syscall for far longer than usual. Because it emits no request traces of its own, the call graph can only point at its nearest traced caller, so the traces accuse a victim and only the kernel identifies the real culprit.",
    "symptoms": [
      "caller latency inflates sharply, often by 10x or more",
      "the slow call edges all converge on one shared downstream component",
      "that component is resource-quiet: CPU flat or falling, no disk or memory pressure",
      "requests still succeed, so error rates barely move"
    ],
    "discriminators": [
      {
        "signal": "blocking-syscall duration of the suspect component",
        "this_problem": "ONE socket-waiting syscall inflates by an order of magnitude while everything else stays flat",
        "not_this_problem": "no syscall inflates by more than about 2x",
        "confused_with": [
          "cpu-contention-co-tenant"
        ],
        "evidence": "MEASURED from raw L0 via babeltrace2: the datastore poll p95 went 15.96 -> 587.70 ms = 36.83x (n=17,245). CONTROL, co-tenant run: the SAME syscall on the SAME component went 16.64 -> 18.72 ms = 1.12x (n=45,337). See evidence/L0_blocking_syscall_slowdb.json and evidence/L0_blocking_syscall_CONTROL_cotenant.json"
      },
      {
        "signal": "runqueue delay of the suspect",
        "this_problem": "flat - the component is not waiting for a CPU, it is waiting for a reply",
        "not_this_problem": "runqueue delay inflates several-fold across many processes",
        "confused_with": [
          "cpu-contention-co-tenant"
        ],
        "evidence": "MEASURED: on the slow-datastore run the datastore runqueue delay was 1.09x and the median across 38 high-volume processes was 0.84x, i.e. unchanged. On the co-tenant run the busiest application process was 7.12x. See evidence/L0_runqueue_delay_slowdb.json"
      },
      {
        "signal": "call-graph convergence",
        "this_problem": "slow edges converge on ONE component that has no slow outgoing edges of its own",
        "not_this_problem": "nothing converges; edge slowdowns stay near 1x",
        "confused_with": [
          "cpu-contention-co-tenant"
        ],
        "evidence": "MEASURED: convergence on a single component at 717.7x incoming with zero slow outgoing edges. CONTROL, co-tenant run: converged_on = null, max 1.7x. See evidence/L0_edge_convergence_slowdb.json"
      }
    ],
    "unverified_do_not_claim": [
      {
        "claim": "the converged-on component shows dominant off-CPU external I/O wait, and that is what identifies it",
        "status": "TRUE BUT NOT DISCRIMINATIVE",
        "measurement": "off_cpu_io_wait is 98-99% for EVERY fault family measured (slow_db 99.4, dependency_outage 98.2, svc_cpu_cap 98.0). The coarse buckets are dominated by idle waiting, so they cannot separate anything. See evidence/wait_signature_all_families.json",
        "consequence": "replaced by the family_seconds split, which is where any real signal lives.",
        "agent_note": "A dominant off-CPU external-I/O SHARE on the suspect. Measured at 98-99% for every labelled fault, because that share is dominated by ordinary idle waiting. Use the DURATION of the specific blocking syscall instead."
      }
    ],
    "known_limitation": {
      "what": "the call graph names a VICTIM, not the culprit",
      "detail": "The injected fault was on the datastore, but convergence lands on its caller because the datastore emits no request spans. Traces get you one hop away; only the kernel evidence identifies the component that is actually blocked.",
      "evidence": "MEASURED: ground truth target was the datastore, convergence returned its caller at 717.7x. See evidence/L0_edge_convergence_slowdb.json"
    }
  },
  "reproduction": {
    "system": "Sock Shop or Train Ticket on Ubuntu 24.04, LTTng 2.15, Docker 27+, single GCP n2 VM. Toxiproxy sits permanently in the datastore path so latency can be toggled without restarting anything.",
    "recipe": "microservice-lttng-data-collection-scripts/faults/slow_db.sh",
    "invoke": "./slow_db.sh inject aggressive",
    "teardown": "./slow_db.sh cleanup",
    "labelled_examples": [
      "sockshop/slow_db/slow_db_aggressive_steady_r1",
      "sockshop/slow_db/slow_db_subtle_steady_r1",
      "trainticket/slow_db/tt_slow_db_aggressive_steady_r1"
    ],
    "harness_only": [
      "labelled_examples"
    ]
  },
  "collection_order": {
    "window": {
      "baseline_s": 60,
      "incident_s": 120,
      "recovery_s": 60
    },
    "kernel_events": [
      "sched_waking",
      "sched_switch",
      "syscall_entry_poll",
      "syscall_exit_poll",
      "syscall_entry_recvfrom",
      "syscall_exit_recvfrom",
      "syscall_entry_epoll_wait",
      "syscall_exit_epoll_wait"
    ],
    "metrics": [
      "container_cpu_usage_seconds_total",
      "container_fs_reads_bytes_total",
      "container_fs_writes_bytes_total",
      "container_network_receive_bytes_total"
    ],
    "logs": [
      "error-rate change per container, to confirm calls succeed rather than fail"
    ],
    "traces": [
      "caller to callee edge latency, baseline versus incident, so convergence can be seen"
    ],
    "why_these": "MEASURED BASIS. The verdict rests on how long the component blocks inside its socket-waiting syscall, so entry AND exit of those calls are required - entry alone gives no duration. The two scheduler events are needed for the negative control: showing runqueue delay is FLAT is what rules out CPU starvation. This is strictly more than the CPU-contention blueprint needs, and that difference is the point: the two problems genuinely require different collection sequences."
  },
  "processing": [
    {
      "step": "Make the kernel trace readable",
      "capability": "trace.stage_ctf",
      "produces": "<tmp>/ctf",
      "expect": "a CTF directory with metadata and channel streams"
    },
    {
      "step": "Find which component the slow call paths converge on",
      "capability": "traces.call_graph.convergence",
      "produces": "<out>/convergence.json",
      "expect": "one component with slow incoming and no slow outgoing edges; it may be the CALLER of the culprit if the culprit emits no spans",
      "reads": [
        "<run_dir>/otlp"
      ]
    },
    {
      "step": "Measure how long the suspect blocks inside each syscall",
      "capability": "kernel.syscall.blocking_duration",
      "produces": "<out>/blocking.json",
      "expect": "one socket-waiting syscall inflated by roughly an order of magnitude",
      "reads": [
        "<tmp>/ctf"
      ]
    },
    {
      "step": "NEGATIVE CONTROL: confirm the suspect is not merely CPU-starved",
      "capability": "kernel.scheduler.runqueue_delay",
      "produces": "<out>/rq.json",
      "expect": "runqueue delay flat; if it inflates broadly this is the CPU-contention blueprint instead",
      "reads": [
        "<tmp>/ctf"
      ]
    },
    {
      "step": "Combine into the verdict and its artifacts",
      "capability": "verdict.dependency_wait",
      "produces": "<out>/verdict.json, <out>/blocking.svg, <out>/explanation.txt",
      "expect": "a named component, the syscall it blocked in, its inflation factor, and the flat runqueue delay that rules out CPU starvation",
      "reads": [
        "<out>/convergence.json",
        "<out>/blocking.json",
        "<out>/rq.json"
      ]
    },
    {
      "step": "State the recommended action alongside the diagnosis",
      "capability": "verdict.dependency_wait",
      "produces": "<out>/recommended_action.txt",
      "expect": "an action aimed at the blocked component or its dependency, explicitly not at the victims named by the call graph"
    }
  ],
  "outputs": [
    {
      "kind": "json",
      "path": "<out>/verdict.json",
      "contains": "the blocked component, the syscall and its inflation, the convergence point, and the runqueue-delay control"
    },
    {
      "kind": "xy_chart",
      "path": "<out>/blocking.svg",
      "contains": "syscall duration p95 baseline vs incident, per component and syscall"
    },
    {
      "kind": "text",
      "path": "<out>/explanation.txt",
      "contains": "what was waiting, on what, and why its callers are victims"
    },
    {
      "kind": "text",
      "path": "<out>/recommended_action.txt",
      "contains": "what to do about it: investigate the named component's own downstream dependency or the path between it and its callers, not the callers themselves"
    }
  ],
  "decision": {
    "verdict_when": [
      "one component socket-waiting syscall inflates by roughly an order of magnitude",
      "that component runqueue delay stays flat, so it is not short of CPU",
      "slow call edges converge on it or on its nearest traced caller",
      "traffic continues to succeed, so error rates barely move"
    ],
    "rule_out": [
      {
        "instead": "co-tenant CPU contention",
        "when": "runqueue delay inflates broadly across many processes while no syscall inflates much"
      },
      {
        "instead": "a frozen dependency",
        "when": "calls fail or hang to timeout instead of returning slowly"
      },
      {
        "instead": "host disk saturation",
        "when": "the inflated wait is a disk syscall and other disk users degrade too"
      }
    ],
    "root_cause_is": "the converged-on datastore component itself, never its callers, which are victims",
    "fault_type": "slow_db"
  },
  "evidence_from_literature": [
    {
      "claim": "Blocking-syscall attribution distinguishes a component that is slow from a component that is waiting, which utilisation metrics cannot do",
      "source": "off-CPU analysis practice (Gregg)",
      "validated_on": "our labelled slow-datastore runs; on Sock Shop this verdict survives even with traces removed entirely"
    }
  ],
  "related_blueprints": [
    "cpu-contention-co-tenant"
  ],
  "mutually_exclusive_with": [
    "cpu-contention-co-tenant"
  ],
  "provenance": {
    "authored_by": "human",
    "verified_by": "PENDING - Naser to sign off",
    "authored_utc": "2026-08-27",
    "notes": "v3: rebuilt on the raw kernel trace. The decisive measurement is a single syscall duration - 36.83x here versus 1.12x for the same syscall on the same component under the CPU fault. Also records the honest limitation that the call graph names the victim, because the culprit emits no spans."
  },
  "taxonomy": {
    "domain": "Performance",
    "category": "Latency degradation",
    "subcategory": "Downstream dependency / datastore bottleneck",
    "sibling_causes": [
      "network latency",
      "queueing delay",
      "thread starvation"
    ],
    "note": "Authored from labelled slow-datastore runs, but written for any case where a component is blocked waiting on something below the traced layer."
  },
  "applicability": {
    "apply_when": [
      "caller latency inflates sharply while error rates stay flat",
      "the slow call paths all end at the same downstream component",
      "that component is not busy"
    ],
    "do_not_apply_when": [
      "calls fail or hang to timeout rather than returning slowly",
      "every hop slows by a similar factor, which points at the network",
      "many unrelated processes are delayed together, which points at CPU contention"
    ],
    "cheap_precheck": "one component has slow incoming edges and no slow outgoing edges"
  },
  "capabilities_required": [
    {
      "id": "trace.stage_ctf",
      "why": "the stored kernel trace must be readable"
    },
    {
      "id": "traces.call_graph.convergence",
      "why": "locate the component the slow paths converge on"
    },
    {
      "id": "kernel.syscall.blocking_duration",
      "why": "the decisive signal: which syscall the suspect blocks in, and for how long"
    },
    {
      "id": "kernel.scheduler.runqueue_delay",
      "why": "negative control: showing runqueue delay is flat rules out CPU starvation"
    },
    {
      "id": "verdict.dependency_wait",
      "why": "combine the three into one verdict"
    }
  ],
  "stopping_conditions": {
    "conclude": "one component's socket-waiting syscall is inflated by roughly an order of magnitude while its runqueue delay stays flat",
    "stop_and_switch": "runqueue delay is broadly inflated instead -> use the CPU-contention blueprint",
    "stop_insufficient": "the suspect emits no spans AND no kernel trace is available for it -> the culprit cannot be identified from the traced layer alone; request kernel collection for that component",
    "max_evidence_rounds": 2
  },
  "adaptation_rules": [
    {
      "if": "convergence names a component that itself has slow outgoing edges",
      "then": "it is a pass-through victim: follow its slow edge and re-run convergence one hop deeper"
    },
    {
      "if": "the inflated wait is a disk syscall rather than a socket syscall",
      "then": "this is storage, not a downstream dependency; check whether other disk users degraded too"
    },
    {
      "if": "no component emits spans on the suspected path",
      "then": "fall back to kernel evidence alone and identify the blocked component by syscall duration, accepting that the call graph cannot confirm it"
    }
  ],
  "selection": {
    "supported_goals": [
      "diagnose_latency_degradation",
      "diagnose_dependency_bottleneck",
      "diagnose_datastore_slowdown"
    ],
    "supported_environments": [
      "linux",
      "container",
      "virtual_machine"
    ],
    "applicable_symptoms": [
      "sharp_caller_latency_increase_without_errors",
      "slow_paths_converge_on_one_component",
      "suspect_component_is_resource_quiet"
    ],
    "preconditions": [
      "distributed traces with parent links, or kernel events for the suspect",
      "syscall entry and exit are collectable for the suspect process"
    ],
    "estimated_overhead_pct": {
      "min": 0,
      "max": 8
    },
    "historical_effectiveness": {
      "runs_validated": 1,
      "note": "one labelled run so far; effect size very large (36.8x vs 1.12x control) but repeats owed"
    }
  },
  "policies": {
    "collection_order_rule": "Use evidence that already exists before enabling any new collection. Escalate a tier only when the cheaper tier leaves a candidate cause unresolved, and record why.",
    "max_collection_overhead_pct": 8,
    "scope": {
      "restrict_to": "the hosts and time window named in the intent",
      "never_widen_without": "explicit approval"
    },
    "privacy": [
      "do not collect request payloads",
      "do not retain personally identifiable data",
      "preserve request identifiers only where permitted"
    ],
    "approval": {
      "automatic": [
        "query evidence that already exists",
        "read-only analysis of collected evidence",
        "active collection estimated below 3% overhead"
      ],
      "requires_approval": [
        "active collection estimated above 3% overhead",
        "widening the host scope or time window",
        "any change to production configuration",
        "any remediation action"
      ]
    },
    "autonomy_level": 2,
    "autonomy_note": "Level 2 = policy-controlled automation: select and execute low-risk plans inside the stated constraints. Level 3 (adaptive escalation within the allowed action space) is the next target; closed-loop remediation is explicitly out of scope.",
    "overhead_note": "Syscall entry+exit tracing is the expensive step here, so this blueprint carries a higher ceiling than the CPU-contention one AND must scope collection to the suspect processes rather than the whole host."
  },
  "evidence_sufficiency": {
    "confidence_floor": 0.7,
    "if_below_floor": "name the unresolved question, pick the ONE additional capability that would settle it, check it against the overhead budget, and request it. Do not broaden collection generally.",
    "report_when_stuck": "if the floor is still not met after the allowed rounds, report the best-supported hypothesis, its confidence, and precisely what evidence is missing - never present a guess as a diagnosis"
  }
}