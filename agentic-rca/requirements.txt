# agentic-RCA harness deps. Data stack (pandas, pyarrow) comes from stratatrace (pip install -e ../stratatrace).
anthropic>=0.40        # RCA_PROVIDER=claude
openai>=1.40           # RCA_PROVIDER=azure | gemini | openai | ollama (all OpenAI-compatible chat.completions)
mcp[cli]>=1.2          # only if serving tools over MCP (see config.py / README §tool-interface)
# ollama: no extra package (OpenAI-compatible HTTP at http://localhost:11434/v1)
